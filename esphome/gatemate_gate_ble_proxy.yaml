esphome:
  name: gatemate-gate-ble-proxy
  friendly_name: GateMate Gate BLE Proxy

esp32:
  board: esp32dev
  framework:
    type: arduino

logger:

api:

ota:

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

captive_portal:

bluetooth_proxy:
